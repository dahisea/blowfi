{
  "name": "blowfi",
  "version": "1.0.0",
  "scripts": {
    "build": "./node_modules/.bin/hugo --gc --minify",
    "postinstall": "git submodule update --init && hugo mod get github.com/nunocoracao/blowfish/v2@v2.90.0",
    "dev": "hugo server -D",
    "clean": "rm -rf public",
    "preview": "hugo server"
  },
  "dependencies": {
    "hugo-extended": "^0.148.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}